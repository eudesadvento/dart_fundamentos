void main() {
// temos 6 tipos
//== (Igualdade), !=(difernça), > ( Maior que), <= (Menor Igual), >= (Maior Igual)
// Vamos ao Ex:.

  final idade = 20;
  final tipoDeCarro = 'honda';

  if (idade == 20) {
    print('habilitado');
  }
  if (idade > 18) {
    print('habilitado');
  }
  if (idade >= 20) {
    print('habilitado');
  }
  if (tipoDeCarro != 'fiat') {
    print('perdão não atende a regra');
  }
}
